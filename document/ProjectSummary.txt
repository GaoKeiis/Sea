1）.在这个项目中 没有充分了解以及去运用组件的可复用性 ，在json数据传参是对参数的值不太了解，没有运用到，导致运用大量的HTTP去清求数据，造成流量的流失。
2）运用组件与组建之间的各种父向子、子向父、兄弟之间的联系以及传递参数的问题（比如在子级创建一个借口、父级通过components来接受借口）

3）vue生命周期的不熟悉，导致做了很多弯路 模拟DOM元素时created是不可以在正式DOM上操作的，通过methods这个函数会等到DOM元素渲染完来操作

4）在登陆页面中。对获取DOM或为多次获取不到或为空的情况（通过methods来解决的这个问题）